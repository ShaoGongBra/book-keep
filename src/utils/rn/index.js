import { noop } from '@/utils'

export const wechatInit = noop
export const codePushHigh = app => app
export const systemUploadApp = noop
export const updateApp = noop
export const startHide = noop
export const duxPushInit = noop
export const crequestMultiplePermission = async () => { }
